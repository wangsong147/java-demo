server:
  port: 9999
spring:
  mail:
    host: smtp.163.com
    username: song5_wang@163.com
    password: FIUECJDZXERBYFNZ
  datasource:
    dynamic:
      primary: master
      datasource:
        master:
          driver-class-name: org.postgresql.Driver
          url: jdbc:postgresql://localhost:5432/postgres?currentSchema=wstest&characterEncoding=utf-8&serverTimezone=GMT%2B8
          username: postgres
          password: ws810216
    # 修改数据库密码：alter user admin with password 'postgres';
        slave_1:
          driver-class-name: org.postgresql.Driver
          url: jdbc:postgresql://localhost:5432/postgres?currentSchema=wstest&characterEncoding=utf-8&serverTimezone=GMT%2B8
          username: postgres
          password: ws810216
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    # 全局配置，也可以通过配置类来配置 public GlobalConfig globalConfig() { return new GlobalConfig(); }
  global-config:
    db-config:
      # 拼接sql时会加上前缀t_: select t_person
      table-prefix: t_
#      id-type: input
